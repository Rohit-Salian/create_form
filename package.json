{
  "name": "form-builder",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "npm@10.5.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "dependencies": {
    "turbo": "^2.5.5"
  }
}
